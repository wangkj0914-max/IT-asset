# 🚀 固定资产系统 - 功能增强计划

## 优先级排序

### 🔥 P0 - 核心业务流程（立即实现）
1. **资产领用/归还流程**
2. **维修和报废管理**

### 📊 P1 - 数据可视化（近期实现）
3. **统计报表（图表）**

### 🏷️ P2 - 便捷功能（中期实现）
4. **二维码生成**
5. **批量导入导出**

---

## 详细实现方案

### 1️⃣ 资产领用/归还流程

#### 数据库设计
```sql
-- 资产领用记录表（已有，需扩展）
ALTER TABLE `asset_use_record` ADD COLUMN `department` VARCHAR(100) COMMENT '领用部门';
ALTER TABLE `asset_use_record` ADD COLUMN `contact_person` VARCHAR(50) COMMENT '联系人';
ALTER TABLE `asset_use_record` ADD COLUMN `contact_phone` VARCHAR(20) COMMENT '联系电话';

-- 资产状态变更
-- status: 0-未领用 1-已领用 2-维修中 3-已报废
```

#### 后端接口
- `POST /asset/use/apply` - 领用申请
- `POST /asset/use/approve` - 领用审批
- `POST /asset/return` - 资产归还
- `GET /asset/use/records` - 领用记录查询

#### 前端页面
- 领用申请对话框
- 我的领用列表
- 待审批列表（管理员）
- 归还确认

---

### 2️⃣ 维修和报废管理

#### 数据库设计
```sql
-- 维修记录表（已有）
-- asset_repair_record: repair_id, asset_id, repair_reason, repair_cost, repair_status, remark

-- 报废记录表（已有）
-- asset_scrap_record: scrap_id, asset_id, scrap_reason, scrap_date, approve_user, remark
```

#### 后端接口
**维修管理**
- `POST /asset/repair/apply` - 报修申请
- `POST /asset/repair/update` - 维修状态更新
- `GET /asset/repair/list` - 维修记录列表

**报废管理**
- `POST /asset/scrap/apply` - 报废申请
- `POST /asset/scrap/approve` - 报废审批
- `GET /asset/scrap/list` - 报废记录列表

#### 前端页面
- 报修申请表单
- 维修进度跟踪
- 报废申请对话框
- 报废审批列表

---

### 3️⃣ 统计报表（图表）

#### 技术选型
- **ECharts** - 百度开源图表库
- **Vue-ECharts** - Vue3 组件封装

#### 统计维度
1. **资产分类统计**（饼图）
   - 各类资产数量占比
   - 各类资产价值占比

2. **资产状态统计**（环形图）
   - 未领用/已领用/维修中/已报废

3. **月度趋势分析**（折线图）
   - 每月新增资产数量
   - 每月资产领用数量

4. **部门资产分布**（柱状图）
   - 各部门资产数量
   - 各部门资产价值

#### 后端接口
- `GET /asset/statistics/category` - 分类统计
- `GET /asset/statistics/status` - 状态统计
- `GET /asset/statistics/trend` - 趋势分析
- `GET /asset/statistics/department` - 部门分布

#### 前端页面
- 数据统计仪表盘
- 可切换的报表视图
- 数据导出功能

---

### 4️⃣ 二维码生成

#### 技术选型
- **qrcode.vue** - Vue3 二维码组件
- 或 **qrcode.js** - 纯 JS 库

#### 功能设计
1. **单资产二维码**
   - 资产基本信息
   - 快速访问链接
   - 扫码查看详情

2. **批量生成**
   - 选择多个资产
   - 批量下载 PNG
   - 支持打印模板

#### 后端接口
- `GET /asset/qrcode/{assetId}` - 获取二维码数据
- `POST /asset/qrcode/batch` - 批量生成

#### 前端页面
- 二维码预览对话框
- 批量生成工具
- 打印模板选择

---

### 5️⃣ 批量导入导出

#### 导入功能
**技术实现**
- 前端：`xlsx` 库解析 Excel
- 后端：`Apache POI` 或 `EasyExcel`

**功能设计**
- Excel 模板下载
- 数据预览和校验
- 错误提示和修正
- 批量导入确认

#### 导出功能（增强版）
**技术实现**
- 前端：`xlsx` 库生成 Excel
- 或后端：`EasyExcel` 流式导出

**功能设计**
- 自定义导出字段
- 筛选条件导出
- 多 Sheet 导出（按分类）
- 带格式的报表

#### 后端接口
- `POST /asset/import` - 批量导入
- `GET /asset/export/template` - 下载模板
- `POST /asset/export` - 自定义导出

#### 前端页面
- 导入向导（3 步）
- 导出配置对话框

---

## 开发计划

### 第一阶段（1-2 天）：核心流程
- [ ] 领用/归还功能
- [ ] 维修管理
- [ ] 报废管理

### 第二阶段（1 天）：统计报表
- [ ] ECharts 集成
- [ ] 4 个统计图表
- [ ] 数据仪表盘

### 第三阶段（1 天）：便捷功能
- [ ] 二维码生成
- [ ] 批量导出（增强）
- [ ] 批量导入

---

## 文件结构变更

```
固定资产系统/
├── it-asset-system/
│   └── src/main/java/com/asset/itassetsystem/
│       ├── Controller/
│       │   ├── AssetUseController.java      # 领用管理
│       │   ├── AssetRepairController.java   # 维修管理
│       │   ├── AssetScrapController.java    # 报废管理
│       │   └── StatisticsController.java    # 统计报表
│       ├── Service/
│       │   ├── AssetUseService.java
│       │   ├── AssetRepairService.java
│       │   ├── AssetScrapService.java
│       │   └── StatisticsService.java
│       └── dto/
│           ├── UseApplyDTO.java
│           ├── RepairDTO.java
│           └── ScrapDTO.java
│
└── it-asset-frontend/
    └── src/
        ├── views/
        │   ├── AssetUse.vue          # 领用管理
        │   ├── AssetRepair.vue       # 维修管理
        │   ├── AssetScrap.vue        # 报废管理
        │   └── Statistics.vue        # 统计报表
        ├── components/
        │   ├── QrcodeDialog.vue      # 二维码对话框
        │   ├── ImportWizard.vue      # 导入向导
        │   └── ExportConfig.vue      # 导出配置
        └── utils/
            └── excel.js              # Excel 工具
```

---

## 立即开始？

回复以下任一指令开始实现：

- **`开始领用功能`** - 实现资产领用/归还
- **`开始统计报表`** - 实现数据可视化
- **`全部实现`** - 按顺序实现所有功能
- **`先看设计`** - 查看详细的数据库设计和 API 设计

---

**预计总工作量**: 3-5 天  
**代码量**: 约 3000-5000 行  
**新增文件**: 约 20 个

🦞 告诉我你想先实现哪个功能！
