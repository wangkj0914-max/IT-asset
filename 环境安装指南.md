# 📦 环境安装指南

## 需要安装的软件

### 1. JDK 17（必需）

**下载地址**: https://adoptium.net/temurin/releases/?version=17

选择：Windows x64 MSI 安装包

**安装步骤**:
1. 下载 `OpenJDK17U-jdk_x64_windows_hotspot_17.x.x.msi`
2. 双击运行，一路 Next
3. 默认安装路径：`C:\Program Files\Eclipse Adoptium\jdk-17.x.x`
4. 安装完成后**重启电脑**（或手动添加环境变量）

**验证安装**:
```bash
# 打开新的命令行窗口
java -version
javac -version
```

应该显示：
```
openjdk version "17.x.x"
```

### 2. Node.js 18（如果前端依赖未安装）

**下载地址**: https://nodejs.org/

选择：LTS 版本（18.x）

**安装步骤**:
1. 下载 LTS 安装包
2. 双击运行，一路 Next
3. 安装完成后重启命令行

**验证安装**:
```bash
node -v
npm -v
```

---

## 手动配置 JAVA_HOME（如果需要）

如果安装后 `java -version` 仍无法识别：

### 方式 1：图形界面配置
1. 右键"此电脑" → 属性 → 高级系统设置
2. 点击"环境变量"
3. 在"系统变量"中点击"新建"
4. 变量名：`JAVA_HOME`
5. 变量值：`C:\Program Files\Eclipse Adoptium\jdk-17.x.x`（实际安装路径）
6. 找到 `Path` 变量 → 编辑 → 新建 → 添加 `%JAVA_HOME%\bin`
7. 确定保存
8. **重启命令行窗口**

### 方式 2：PowerShell 配置（临时）
```powershell
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-17.x.x"
$env:Path = "$env:JAVA_HOME\bin;$env:Path"
```

---

## 安装完成后启动系统

### 方式 1：双击启动脚本
```
F:\固定资产系统\启动.bat
```

### 方式 2：命令行启动
```bash
# 1. 启动后端
cd F:\固定资产系统\it-asset-system
.\mvnw.cmd spring-boot:run

# 2. 启动前端（新窗口）
cd F:\固定资产系统\it-asset-frontend
npm run serve
```

---

## 常见问题

### Q: 下载太慢？
**A**: 使用国内镜像：
- JDK: https://mirrors.aliyun.com/adoptium/
- Node.js: https://npmmirror.com/mirrors/node/

### Q: 安装后 java 命令仍不可用？
**A**: 
1. 确认安装成功：检查安装目录是否存在
2. 重启电脑
3. 打开**新的**命令行窗口测试

### Q: Maven 下载依赖太慢？
**A**: 修改 Maven 镜像（项目已配置使用阿里云镜像）

---

**安装完成后，运行 `启动.bat` 即可！**
