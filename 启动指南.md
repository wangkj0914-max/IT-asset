# 🚀 启动指南

## 第一步：初始化数据库

1. 确保 MySQL 已安装并运行
2. 修改数据库配置（如果需要）：
   - 打开 `database\init.sql`
   - 确认数据库用户名密码（默认：root / CHNX#000）

3. 执行 SQL 脚本：
   ```bash
   # 方式 1: 命令行
   mysql -u root -p < database\init.sql
   
   # 方式 2: MySQL Workbench / Navicat
   # 打开 init.sql 文件并执行
   ```

## 第二步：配置后端

1. 打开 `it-asset-system\src\main\resources\application.yml`
2. 确认数据库连接配置：
   ```yaml
   spring:
     datasource:
       url: jdbc:mysql://localhost:3306/it_asset_manage?...
       username: root
       password: 你的密码
   ```

3. 启动后端：
   ```bash
   cd it-asset-system
   
   # 方式 1: 直接运行（开发模式）
   .\mvnw.cmd spring-boot:run
   
   # 方式 2: 打包后运行
   .\mvnw.cmd clean package
   java -jar target\it-asset-system-0.0.1-SNAPSHOT.jar
   ```

4. 验证后端启动成功：
   - 访问：http://localhost:8080/asset/login
   - 看到登录接口说明后端正常

## 第三步：配置前端

1. 安装 Node.js（如果未安装）：
   - 下载地址：https://nodejs.org/
   - 推荐版本：16.x 或 18.x

2. 安装依赖：
   ```bash
   cd it-asset-frontend
   npm install
   ```

3. 启动前端：
   ```bash
   npm run serve
   ```

4. 访问系统：
   - 浏览器打开：http://localhost:8081
   - 默认账号：`admin` / `123456`

## 常见问题

### 1. 后端启动失败：`Port 8080 was already in use`
**解决**: 修改端口
```yaml
# application.yml
server:
  port: 8081  # 改为其他端口
```

### 2. 前端启动失败：`npm install` 报错
**解决**: 
```bash
# 清理缓存
npm cache clean --force

# 删除 node_modules 重新安装
rm -rf node_modules package-lock.json
npm install

# 或使用淘宝镜像
npm config set registry https://registry.npmmirror.com
npm install
```

### 3. 数据库连接失败
**解决**:
- 检查 MySQL 是否运行
- 确认用户名密码正确
- 确认数据库 `it_asset_manage` 已创建

### 4. 前端请求后端 404
**解决**: 检查 `request.js` 中的 `baseURL` 是否与后端端口一致
```javascript
baseURL: 'http://localhost:8080/asset'  // 确保端口正确
```

### 5. 跨域错误（CORS）
**解决**: 后端已配置跨域，如仍有问题检查：
- 后端 `CorsConfig.java` 是否生效
- 前端请求是否携带正确的 headers

## 测试登录

1. 打开浏览器访问前端地址
2. 输入账号密码：
   - 用户名：`admin`
   - 密码：`123456`
3. 点击登录
4. 成功进入资产管理页面

## 下一步

登录成功后，你可以：
- ✅ 新增资产（点击"新增资产"按钮）
- ✅ 查看资产列表
- ✅ 搜索和筛选资产
- ✅ 编辑和删除资产
- ✅ 导出资产数据（CSV 格式）

---

**需要帮助？** 查看 `README.md` 了解更多功能说明。
